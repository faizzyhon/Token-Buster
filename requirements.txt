# Token Buster — Requirements
# Author: Muhammad Faizan | github.com/faizzyhon | instagram.com/faizzyhon
#
# Install: pip install -r requirements.txt

# Web framework
flask>=3.0.0
flask-cors>=4.0.0

# Tokenization (choose one or both)
tiktoken>=0.6.0              # Recommended — fast, OpenAI/Claude compatible
# transformers>=4.40.0       # Optional — HuggingFace models (heavier)

# File monitoring
watchdog>=4.0.0

# Utilities
python-dotenv>=1.0.0         # .env file support
